# API Configuration
VITE_API_BASE_URL=https://your-api-domain.com/api
VITE_CHAT_ENDPOINT=/chat
VITE_PRODUCTS_SEARCH_ENDPOINT=/products-search
VITE_VECTORIZE_ENDPOINT=/vectorize-products

# Development mode - set to true to use mock data instead of API
VITE_DEV_MODE=false

# Fallback URLs (если основные API недоступны)
VITE_FALLBACK_API_URL=http://localhost:8787/api